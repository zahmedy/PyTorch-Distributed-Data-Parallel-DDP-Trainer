# PyTorch-Distributed-Data-Parallel-DDP-Trainer

```
ddp-trainer/
│
├── train_ddp.py    ← main entrypoint (run with torchrun)
├── model.py        ← simple CNN/Transformer model
├── data.py         ← dataset + dataloader + sampler
├── utils.py        ← setup/cleanup + save/resume
└── README.md       ← notes + how to run
```